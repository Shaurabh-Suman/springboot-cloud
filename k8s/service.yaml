apiVersion: v1
kind: Service

metadata:
  name: spring-cloud-service

spec:
  type: NodePort

  selector:
    app: spring-cloud-app

  ports:
    - port: 8088
      targetPort: 8088
      nodePort: 30088